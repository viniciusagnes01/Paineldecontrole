# Correção Alphaville - fallback de CRM e Mídia

Aplique com:

```bash
git apply alphaville-pull-data-fallback.patch
git add .
git commit -m "Fix Alphaville CRM and media fallback"
git push origin V4-COMPANY
```

Depois faça hard refresh no navegador: Ctrl + Shift + R.
